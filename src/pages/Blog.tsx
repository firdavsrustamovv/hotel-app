import Header from "../components/Header";

type Props = {};

const Blog = (props: Props) => {
  return (
    <div style={{ background: "black" }}>
      <Header />
      Blog
    </div>
  );
};

export default Blog;
